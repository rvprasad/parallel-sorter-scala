Implementation of parallel/distributed external sorting capable of handling
data that does not fit into memory (RAM).

- **Single-node-multi-core** implementation utilizes multiple cores on a single
  node.
